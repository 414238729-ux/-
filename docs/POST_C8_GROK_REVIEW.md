# 长测前 Grok 独立审查及处理

2026-09-08信息机制修正说明：下面保留的是**此前快照**的审查与当时处理。
其中自动桃/杀信号的旧前提已被用户后续澄清替代；当前版本改为独立的公共无懈知识和公开场景问答。
本轮信息修正未再次调用Grok，不宣称已有独立复审；最终字节审查仍等待真实长测结果。
旧文件完整字节已存于`post_c8_evidence/information_revision_01/prior/docs/POST_C8_GROK_REVIEW.md`。

本记录是长测前代码审查，不是最终字节验收。完整审查输出、输入字节及SHA、调用参数、PID、退出码均保留在 [grok_pre_long_02](post_c8_evidence/grok_pre_long_02/grok.json) 与 [原始输出](post_c8_evidence/grok_pre_long_02/grok.log)。

首次调用 `grok_pre_long_01`：Grok PID 50968，真实exit code 1。沙箱无法让CLI读取自己的既有认证状态；没有读取或发送凭据内容。

按用户授权在许可环境重试 `grok_pre_long_02`：Grok PID 58656，真实exit code 0，完整返回审查正文。审查用时896.46秒。结论原文为 `NO_BLOCKING_FINDINGS_IN_REVIEWED_SCOPE`，同时提出两个稳健性／完整度缺陷，并明确未覆盖引擎其余正文、模式policy内部、技能实现或长测。

审查输入绑定当时源码包 `a4b7618ccb6569f2a4745f656164438a6cf02ae0f7afc601cbd1c60007edd73d`。审查期间本任务继续修改自己创建的运行层／AI源码，因此包装脚本如实记录 `source_and_tests_unchanged=false` 并退出1；不能把此审查当成最终源码审查。Grok正文已经完整结束，随后CLI有resident session退出警告；没有据该警告或CLI退出码伪造最终审计PASS。

| 发现或建议 | 本轮处理 | 验证／保留范围 |
|---|---|---|
| 引擎提交后，摘要／内奸统计抛错可能发生在写回执之前 | 摘要在提交前暂存；成功提交后的第一组赋值关闭旧决策并保存回执，随后统计与事件投影受错误停止保护 | 新增提交后统计异常注入，要求重复请求返回回执且引擎只执行一次；原事件投影异常测试继续保留 |
| 杀数信号已投影但AI未使用 | 借刀使用评分区分持械者与受击者，读取可见手牌或已发送的杀数；未知仍为None，不能当0 | 1与0改变策略选择的场景测试；补齐生产借刀payload第二目标的真实候选投影测试 |
| 开局适配器自身没有再次核对当前行动者 | 在开局枚举增加行动者守卫 | 正常签名开局／错误行动者短测 |
| token命中后可再核对生产action.actor_id | 已增加守卫 | 原跨座位、跨会话和过期测试 |
| AI每个决策重复发送同一手牌信号 | AI只在此前未发送或手牌变化后发送；仍只有自己当前决策时的有限信号接口 | 手牌变化过期测试；没有扩张为聊天／客户端送花系统 |
| API不建立账号或座位鉴权 | 与用户明确范围一致，未额外建设登录系统；可信调用方必须传正确座位 | 正常AI只由运行层收到自己的视图；未来若对外提供网络服务，需另行设计 |
| 初始化失败时无参战阵容，分维出场较请求数少 | 保留真实情况；报告明确分维只含已经初始化的参与者，总表仍记全部ERROR | 不从失败前配置虚构实际出场 |
| 自用候选与客户端不同角色候选数／官方池不完全同构 | 已明确声明用户自用等概率候选，不声称官方池／地主五选一等完成 | 本轮只有三名完整生产武将，不扩张开发库 |
| 其余引擎、政策、技能正文及长测不足 | 保留独立验证缺口 | 外部长测返回后，使用最终字节、规则、真实结果补最终独立审查 |

修复由本任务执行，不声称Grok已复审这些后续字节。最终短测记录以 [最终报告](POST_C8_PLAYABLE_REPORT.md) 指向的最后一份原始执行证据为准。
