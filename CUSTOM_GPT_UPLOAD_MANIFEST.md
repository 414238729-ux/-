# 自定义 GPT 唯一上传清单

> 清单状态：当前项目唯一上传清单  
> 当前清单更新：2026-09-09（POST-C8 独立 AI 信息规则登记）
> 历史审计日期：2026-07-28
> 重要：**本文件仅供本地维护，不复制到“指令”框，也不上传到 Knowledge。**

本清单用于避免自定义 GPT 同时检索新旧副本。后台中的旧附件不会因本地文件更新而自动替换，必须先主动删除。

## 1. 复制到“指令”框

只复制以下文件的全文：

1. `GPT_INSTRUCTIONS.md`

操作要求：

- 从文件第一行到最后一行完整复制，整体覆盖自定义 GPT 当前的“指令（Instructions）”内容。
- 不要把 `GPT_INSTRUCTIONS.md` 同时上传到 Knowledge。
- 不要把 `AGENTS.md`、README、上传清单或测试说明混入指令框。

## 2. 上传到 Knowledge

当前唯一正式上传集合为 `knowledge/` 根目录中的以下十三个文件：

1. `knowledge/三国杀模拟规范.md`
2. `knowledge/三国杀模式规则.md`
3. `knowledge/三国杀基础术语与通用机制.md`
4. `knowledge/三国杀卡牌效果.md`
5. `knowledge/三国杀武将规则补充.md`
6. `knowledge/三国杀增量规则整理_第二次汇总之后.md`
7. `knowledge/三国杀卡牌结构化数据.csv`
8. `knowledge/三国杀卡牌使用方式.csv`
9. `knowledge/三国杀牌堆数据.csv`
10. `knowledge/三角洲枪械分析规范.md`
11. `knowledge/Overlord_DND换算规范.md`
12. `knowledge/通用概率分析规范.md`
13. `knowledge/三国杀AI信息规则.md`

除以上十三个路径外，不上传其他文件。若未来建立 `knowledge/archive/` 或其他归档目录，其中内容一律不属于正式上传集合。

## 3. 更新现有自定义 GPT 的安全顺序

1. 打开自定义 GPT 编辑后台的 Knowledge 附件列表。
2. 删除后台中所有旧附件，尤其是：
   - 与上述十三个文件同名的旧附件；
   - 带“旧版”“新版”“副本”“备份”、时间戳、括号序号或类似后缀的历史附件；
   - 内容相同但文件名不同的重复附件。
3. **先保存一次 GPT 配置，确认旧附件已从后台移除。**
4. 再严格按照第 2 节上传十三个当前正式文件，每个文件只上传一次。
5. 再次保存 GPT 配置，并检查 Knowledge 列表中只有这十三个文件且没有重名项。
6. 若 `GPT_INSTRUCTIONS.md` 本轮发生修改，再用其全文整体覆盖“指令”框并保存；从零配置时始终执行该步骤。

不得依靠文件修改时间、文件名中的“最新版”或检索优先级解决冲突；自定义 GPT 的 Knowledge 集合中必须直接移除旧副本。

## 4. 只保留在本地

以下内容用于维护、计算、测试或人工配置，不作为长期 Knowledge 上传：

- `scripts/`
- `tests/`
- `README.md`
- `UPLOAD_GUIDE.md`
- `CUSTOM_GPT_UPLOAD_MANIFEST.md`
- `AGENTS.md`
- `requirements-dev.txt`
- `pytest.ini`
- `.gitignore`
- Python 缓存、pytest 缓存、虚拟环境和版本控制内部文件

本地 Python 文件可以在特定数据分析会话中按需临时附加并明确执行，但这不等于将整个 `scripts/` 目录长期上传为 Knowledge。

## 5. 本地副本审计结果

2026-07-28 对项目目录及其子目录进行了只读审计，并排除了 `.git`、Python 缓存和 pytest 缓存等内部生成内容：

- 九个正式三国杀文件各仅发现一份，且都位于 `knowledge/` 根目录；
- 未发现带时间戳、“新版”“旧版”“副本”“备份”、`copy`、`old` 或 `backup` 后缀的三国杀 Knowledge 副本；
- 未发现内容完全相同的重复项目文件；
- 当前没有需要删除、移动或归档的用户文件，本次审计未删除任何文件。

这项本地审计不能代替自定义 GPT 后台附件检查。后台可能仍保留以前手动上传的旧文件，更新时仍必须执行第 3 节的“先删除、保存、再上传”流程。

## POST-C8 本地证据边界

2026-09-09：当前正式资料增加独立 AI 信息规则，正式上传集合为以上十三个文件。`docs/post_c8_evidence/` 中的历史字节副本与 `.pt/` 的冻结源码测试副本不属于当前正式资料或上传集合；必须保留，不以删除历史来满足唯一性测试。上述2026-07-28审计仅描述当时状态。
