# Codex 项目规则

本文件适用于本仓库全部目录。后续 Codex 任务必须遵循以下要求。

## 资料与证据

1. 不捏造游戏数据、技能文本、规则裁定、版本信息或来源。
2. 规则原文、原文摘要、推断、计算假设和计算结果必须明确分开。
3. 没有可靠来源的内容写为“待填写”“待核验”或“计算假设”，不得用看似精确的占位数值。
4. 新增游戏资料时记录平台、版本、模式、来源位置和核验状态。
5. 模拟结果不得描述成官方事实；置信区间只表示抽样误差。

## 代码与公式

1. 核心概率逻辑必须保持通用，不得把具体游戏数据硬编码到 `scripts` 通用模块中。
2. 所有随机逻辑必须支持固定随机种子，并使用局部随机数生成器。
3. 重复触发逻辑必须具有明确硬上限，并暴露是否因上限而截断。
4. 修改公式、分位数定义、误差计算、标准化规则或排名规则时，必须同步更新测试和相关文档。
5. 对非法输入给出清楚的中文错误信息。
6. 生产代码仅依赖 Python 标准库；测试依赖可以使用 `requirements-dev.txt` 中的工具。
7. 不使用外部付费 API，不把网络服务作为核心计算的必需依赖。

## 测试与完成标准

1. 修改代码后先运行：

   ```powershell
   python -m pytest -q
   ```

2. 必要时同时运行：

   ```powershell
   python -m compileall -q scripts tests
   ```

3. 未实际运行测试时，不得声称任务完成或测试通过。
4. 新增公式或随机机制时，至少加入一个确定性案例和一个边界案例。
5. 能精确枚举的随机机制，应加入模拟与精确结果的误差对照测试。

## 文件安全与沟通

1. 不删除用户资料，不覆盖与当前任务无关的修改。
2. 若确需迁移或移除文件，先说明目标、原因和可恢复方式。
3. 所有重要输出、变更说明、测试结果和限制使用中文。
4. 保持 Markdown 和 Python 文件为 UTF-8 编码。
5. README、Knowledge、Instructions 与实际代码行为必须保持一致。

## 三国杀增量资料

1. 用户个人总结、游戏内观察和用户整理说明不得表述成官方统一规则原文。
2. 三国杀规则资料的状态标签只使用：`当前确认`、`用户整理解释`、`文本推导`、`模拟假设`、`待核验`、`历史规则`、`分析约定`。
3. `三国杀卡牌结构化数据.csv` 的空字段表示本次资料未提供，不表示不存在限制。
4. 模式规则、基础机制、卡牌效果、结构化卡牌效果和实体牌堆数据必须分层维护，不得静默跨平台、跨模式或跨版本合并。
