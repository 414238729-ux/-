# 自定义 GPT 上传与配置指南

本指南用于把本仓库第一版配置为私人 GPT“游戏规则与数值模拟器”。实际操作时以根目录 `CUSTOM_GPT_UPLOAD_MANIFEST.md` 作为唯一上传清单；该清单本身只保留在本地，不上传到 Knowledge。

## 一页结论

| 用途 | 文件 | 操作 |
|---|---|---|
| GPT 行为指令 | `GPT_INSTRUCTIONS.md` | 复制全文，覆盖自定义 GPT 现有的“指令（Instructions）”内容 |
| GPT 参考知识 | `knowledge/三国杀模拟规范.md` | 删除旧版同名文件后上传新版 |
| GPT 参考知识 | `knowledge/三国杀模式规则.md` | 删除旧版同名文件后上传新版 |
| GPT 参考知识 | `knowledge/三国杀基础术语与通用机制.md` | 删除旧版同名文件后上传新版 |
| GPT 参考知识 | `knowledge/三国杀卡牌效果.md` | 删除旧版同名文件后上传新版 |
| GPT 武将规则 | `knowledge/三国杀武将规则补充.md` | 新增上传；用于指定武将的技能原文、项目结算与专属策略 |
| GPT 增量追溯 | `knowledge/三国杀增量规则整理_第二次汇总之后.md` | 新增上传；保存本轮新增、修正、证据状态和仍保留未知 |
| GPT 卡牌数据 | `knowledge/三国杀卡牌结构化数据.csv` | 删除旧版同名文件后上传新版 |
| GPT 卡牌用途 | `knowledge/三国杀卡牌使用方式.csv` | 新增上传；与卡牌定义按 `card_key` 关联 |
| GPT 牌堆数据 | `knowledge/三国杀牌堆数据.csv` | 删除旧版同名文件后上传新版 |
| GPT 参考知识 | `knowledge/三角洲枪械分析规范.md` | 上传到“知识（Knowledge）” |
| GPT 参考知识 | `knowledge/Overlord_DND换算规范.md` | 上传到“知识（Knowledge）” |
| GPT 参考知识 | `knowledge/通用概率分析规范.md` | 上传到“知识（Knowledge）” |
| 本地维护与测试 | 其余全部文件 | 保留在本地，不作为 Knowledge 上传 |

`GPT_INSTRUCTIONS.md` 在复制后也应继续保留在本地，作为以后更新 GPT 指令的唯一维护源。

更新现有 GPT 时，先删除对应旧版同名 Knowledge，避免新旧副本同时被检索。本轮新增 `三国杀增量规则整理_第二次汇总之后.md`；其他发生修改的 Knowledge 也应替换旧附件。若后台已有历史副本，按根目录唯一清单先清空旧附件，再上传全部十二份正式文件。

## 1. 复制到“指令”的文件

### `GPT_INSTRUCTIONS.md`

操作方式：

1. 使用 UTF-8 文本编辑器打开 `GPT_INSTRUCTIONS.md`；
2. 从第一行标题到最后一行全部复制；
3. 直接粘贴到私人 GPT 的“指令（Instructions）”框；
4. 不要在内容外再套 Markdown 代码块；
5. 不要把该文件同时上传为 Knowledge，以免维护时出现两个版本。

验收结论：

- 文件内容是可直接执行的角色、流程、统计和输出约束；
- 没有仓库安装命令、Codex 维护说明或只适用于开发者的测试步骤；
- 原文、原文摘要、推断、计算假设、计算结果和未知项已明确分开；
- 明确禁止捏造游戏数据和伪造模拟执行；
- 使用清晰的 Markdown 标题、表格和编号步骤；
- 文件为有效 UTF-8 文本；
- 可以直接复制，无需删除文件头或附录。

## 2. 上传到“知识”的文件

只上传以下十二个文件：

1. `knowledge/三国杀模拟规范.md`
2. `knowledge/三国杀模式规则.md`
3. `knowledge/三国杀基础术语与通用机制.md`
4. `knowledge/三国杀卡牌效果.md`
5. `knowledge/三国杀武将规则补充.md`
6. `knowledge/三国杀增量规则整理_第二次汇总之后.md`
7. `knowledge/三国杀卡牌结构化数据.csv`
8. `knowledge/三国杀卡牌使用方式.csv`
9. `knowledge/三国杀牌堆数据.csv`
10. `knowledge/三角洲枪械分析规范.md`
11. `knowledge/Overlord_DND换算规范.md`
12. `knowledge/通用概率分析规范.md`

这些文件适合作为 Knowledge，原因是：

- 九份 Markdown 资料是文本导向的，没有复杂排版或仅靠图片表达的信息；
- 三份 CSV 是带表头的结构化文本，分别供 GPT 检索卡牌定义、多用途和实体牌堆；
- 规范包含用途、术语、来源元数据、结构化字段和待补充项；
- 规范中尚无可靠资料的字段继续使用“待填写”“待核验”等占位符；
- 牌堆 CSV 保留初始非官方转录来源，同时明确记录用户对照实卡核验状态；
- 没有擅自补写额外的枪械、角色、版本或规则数值；
- `通用概率分析规范.md` 中的数字只用于通用概率范围、分位点和统计定义，不是游戏数据；
- 十二个文件均为纯文本资料。

OpenAI 的[创建和编辑 GPT 说明](https://help.openai.com/en/articles/8554397)建议把行为、语气和工作流放在 Instructions，把参考资料放在 Knowledge，并优先使用清晰的文本型文件。Markdown 按参考职责分层，三份 CSV 分别提供卡牌定义、卡牌用途与实体牌记录；真正的行为优先级仍由 `GPT_INSTRUCTIONS.md` 决定。

### 三国杀规则与结构化卡牌数据的口径

- 模式规则来源是用户个人总结与游戏内观察，不是官方统一规则原文；
- 基础术语与卡牌效果是用户摘取、修改并整理的通俗说明；
- 模式规则、基础机制和卡牌效果分别保存，查询时先匹配平台、模式和版本；
- `三国杀武将规则补充.md` 仅按需保存当前已提供武将的技能原文、项目采用结算和专属策略，不要求建立完整武将数据库；
- `三国杀增量规则整理_第二次汇总之后.md` 保存本轮增量正文、证据状态和仍保留未知；发生检索冲突时，以用户当前明确修正及已分流到模式、术语、武将和模拟规范的对应正式章节为准；
- `三国杀卡牌结构化数据.csv` 只收录本次提供的38种卡牌，每种一条核心记录；
- `三国杀卡牌使用方式.csv` 保存【桃】【酒】的多用途时机、目标、条件与独立额度，以及【闪】按响应对象区分“使用/打出”的事件映射，不重复卡牌全文；
- 该 CSV 的空字段表示“本资料未提供”，不得解释为没有限制；锦囊的空 `specific_timing` 还必须由 `inherits_generic_timing` 明确继承，不能解释为任意时机；
- 结构化卡牌 CSV 不包含实体牌数量、花色或点数，不能替代牌堆 CSV；
- `文本推导`、`模拟假设` 和 `待核验` 内容在回答中必须主动披露。

### 三国杀牌堆 CSV 的数据口径

- 数量口径：160条实体牌实例、每行 `quantity=1`，合计 160 张；
- 平台、模式、版本和生效日期：以 CSV 每行字段为准；
- 来源：初始为非官方网络UP主转录，后经用户与实际卡牌逐张核对；
- 核验状态：`user_verified_against_actual_cards`；这仍不是官方发布或官方逐牌认证牌表；
- 扩展包口径：默认包含 4 张 EX 扩展牌；其余牌没有逐张细分所属扩展包；
- 宝物口径：当前160张牌堆不包含宝物牌，这不是待补资料；虚拟牌和特殊标记不写入基础牌堆；
- 查询约束：先锁定唯一 `deck_id`，不得把不同模式、平台或版本的记录合并计算。
- 当前 `deck_id` 适用于三国杀移动版常规军争身份模式、排位2v2、斗地主和“主公立储、内奸择途”八人军争特殊玩法；该玩法复用同一160张牌堆，不上传第二份重复 CSV。范围外特殊模式、模式专属牌堆、其他服务器及活动模式需另行核对。

上传 CSV 只让 GPT 能够检索数据，不会自动执行仓库中的 Python 审计与概率函数。若用户要求抽牌、刷牌、颜色、花色、点数或类别概率，GPT 仍须先核对平台、模式、版本和字段完整度。

## 3. 只保留在本地的文件

以下文件或目录不应作为 Knowledge 上传：

### 代码与测试

- `scripts/`
- `tests/`
- `requirements-dev.txt`
- `pytest.ini`

原因：这些文件用于本地计算、导入和自动化测试。Knowledge 是检索型参考资料，不会把 Python 模块自动安装到数据分析环境。

如果某次 ChatGPT 数据分析任务需要直接复用代码，可以在该次会话中单独附加所需 `.py` 文件，并明确要求导入或执行；这不等于把整个代码目录长期上传为 Knowledge。`scripts/deck_data.py` 可在本地加载并审计牌堆 CSV；`scripts/sgs_modes.py` 可验证2v2与斗地主的轻量约束；`scripts/sgs_card_rules.py` 可验证卡牌结算边界与锦囊通用时机继承；`scripts/sgs_chain_strategy.py` 可比较铁索组合的团队净收益和信息限制；`scripts/sgs_extended_rules.py` 可验证牌堆重洗、延时锦囊共存与阶段跳过、存活角色环和军争身份模式；`scripts/sgs_limited_identity_variant.py` 只在显式模式开关下验证移动版八人军争限时变体。

本地调用示例：

```python
from pathlib import Path

from scripts.deck_data import (
    count_by_field,
    load_deck_csv,
    probability_at_least_one_by_field,
)

records, audit = load_deck_csv(
    Path("knowledge/三国杀牌堆数据.csv"),
    expected_total=160,
)
print(audit.total_quantity)
for issue in audit.issues:
    print(issue.severity, issue.code, issue.message)
if not audit.is_valid:
    raise RuntimeError("牌堆审计失败，停止统计与概率计算")

deck_id = records[0].deck_id
counts = count_by_field(
    records,
    deck_id=deck_id,
    field="card_name",
)

result = probability_at_least_one_by_field(
    records,
    deck_id=deck_id,
    field="card_name",
    value="由用户指定的牌名",
    draw_count=2,
)
print(result)
```

该概率使用有限牌堆的不放回组合公式，不是蒙特卡洛估计。即使计算精确，也只对当前用户对照实卡核验后的 CSV 数据口径成立，不表示官方发布。

### 项目维护文件

- `AGENTS.md`
- `README.md`
- `UPLOAD_GUIDE.md`
- `CUSTOM_GPT_UPLOAD_MANIFEST.md`
- `.gitignore`

原因：

- `AGENTS.md` 是后续 Codex 的仓库维护规则；
- `README.md` 是开发与使用说明；
- `UPLOAD_GUIDE.md` 是人工配置清单；
- `CUSTOM_GPT_UPLOAD_MANIFEST.md` 是唯一上传清单与副本审计记录；
- `.gitignore` 只控制本地缓存和虚拟环境文件。

它们不会提高 GPT 对游戏规则的检索质量，也不应与 GPT Instructions 混合。

## 4. 推荐配置步骤

1. 在 ChatGPT 网页端打开 GPT 编辑器；
2. 创建或编辑私人 GPT，并将名称设为“游戏规则与数值模拟器”；
3. 按第 1 节复制 `GPT_INSTRUCTIONS.md` 全文；
4. 按第 2 节上传九份 Markdown 和三份 CSV；
5. 启用“代码解释器与数据分析”能力，以便实际运行 Python 计算；
6. 第一版不配置 Apps、Actions 或外部 API；
7. 在预览中运行验收提示；
8. 确认结果后保存或更新 GPT。

产品界面名称和可用能力可能因账户、工作区或后续产品更新而变化，应以当前编辑器显示和 OpenAI 官方说明为准。

## 5. 预览验收提示

### 精确概率案例

```text
一个袋子中有 2 个成功标记和 2 个失败标记。不放回抽取 2 个。
请计算抽到 0、1、2 个成功标记的精确概率，并分开列出规则原文、
推断和计算假设。
```

期望检查：

- 优先使用组合枚举，不只给蒙特卡洛结果；
- 说明不放回；
- 给出完整概率分布；
- 不添加任何游戏背景或真实游戏数据。

### 歧义案例

```text
规则原文：“当事件发生后，你可以再次执行此效果。”
请分别按“最多再执行一次”和“只要条件满足即可重复”两种解释建模。
不要判断哪一种是官方解释。
```

期望检查：

- 分别标出两种解释；
- 为可能重复的解释设置硬上限；
- 不把任一解释说成官方裁定；
- 若实际模拟，公开试验次数和随机种子。

### 缺失数据案例

```text
请分析某武器的击杀时间，但我还没有提供版本、伤害、射速和目标生命值。
```

期望检查：

- 明确列出缺失数据；
- 不凭记忆填写武器数值；
- 提供所需字段或数据模板，而不是伪造计算结果。

### 三国杀牌堆案例

```text
请使用已上传的三国杀牌堆 CSV，先报告采用的 deck_id、平台、模式、
版本和总牌数，再精确计算不放回抽取时至少获得一张我指定牌名的概率。
不要把“用户已核验”的牌堆说成官方发布或官方逐牌认证数据。
```

期望检查：

- 优先检索 `三国杀牌堆数据.csv`；
- 明确当前数据共 160 张，核验状态为用户对照实卡核验，初始来源并非官方清单；
- 不混用其他平台、模式或版本的牌堆；
- 使用精确不放回概率，并说明结论只对当前 CSV 数据口径成立。

## 6. 更新规则

- 修改 `GPT_INSTRUCTIONS.md` 后，重新复制全文覆盖 GPT 的 Instructions；
- 修改任一 Knowledge 文件后，重新上传对应文件，并确认编辑器中没有旧版本副本；
- 更新 CSV 时同时核对 `quantity` 总和、重复记录、空字段和冲突记录；
- 新增真实游戏资料时，必须记录来源、版本、模式和核验状态；
- 三国杀资料的不确定内容使用项目规定的七种状态标签，不创建同义状态；
- 修改公式或脚本后，先运行完整 pytest，再更新 GPT 配置；
- 在 GPT 预览中重新执行至少一个精确案例和一个歧义案例。

## 7. 当前边界

- 上传这套文件不会自动创建在线 API；
- 本地 Python 脚本不会因成为 Knowledge 而自动执行；
- 当前三国杀牌堆初始来源非官方，已由用户对照实卡逐张核验；默认包含 4 张 EX 扩展牌、不包含宝物牌，其他扩展包来源未逐张细分；
- 当前牌堆只按记录用于移动版常规军争身份、排位2v2、斗地主和“主公立储、内奸择途”八人军争特殊玩法，不外推到范围外特殊或其他服务器牌堆；
- 八人特殊玩法没有已确认的独立正式名称；当前仅记录宣传显示文字、限时开启标签和最后核验日期，未来是否常驻与具体下架时间未知；
- 客户端武将候选实际分布非等概率但具体权重未知；无权重时的等概率候选生成必须明确标为模拟假设，固定阵容不计算刷出概率；
- 需要实时外部数据、长期存储或第三方操作时，必须另行部署 API，并按 OpenAI 的[GPT Actions 配置说明](https://help.openai.com/en/articles/9442513-configuring-actions-in-gpts)提供 OpenAPI Schema 和适当认证；
- 当前版本只提供 Instructions、Knowledge 规范、三份三国杀 CSV、本地轻量计算代码和测试，不包含真正的自定义 Action。
